intf_libs = # Interface dependencies.
impl_libs = # Implementation dependencies.

# stereo.hpp includes only opencv2/core.hpp directly, so core is the
# interface dependency; imgproc and geometry are implementation-only
# (used by the stereo matching algorithms, not exposed in the public API).
#
import intf_libs += libopencv-core%lib{opencv-core}
import impl_libs += libopencv-imgproc%lib{opencv-imgproc}
import impl_libs += libopencv-geometry%lib{opencv-geometry}

# Public headers.
#
pub = [dir_path] ../include/

include $pub

pub_hdrs = $($pub/ pub_hdrs)

lib{opencv-stereo}: $pub/{$pub_hdrs}

# Private headers and sources as well as dependencies.
#
lib{opencv-stereo}: {hxx ixx txx cxx}{**} $impl_libs $intf_libs

# Build options.
#
out_pfx_inc = [dir_path] $out_root/include/
src_pfx_inc = [dir_path] $src_root/include/
out_pfx_src = [dir_path] $out_root/src/
src_pfx_src = [dir_path] $src_root/src/

cxx.poptions =+ "-I$out_pfx_src" "-I$src_pfx_src" \
                "-I$out_pfx_inc" "-I$src_pfx_inc"

# OpenCV's own module sources gate their private headers on this macro
# (upstream's CMake build defines it for every module's own compilation).
#
cxx.poptions += -D__OPENCV_BUILD=1

#obja{*}: cxx.poptions += -DOPENCV2_STATIC_BUILD
#objs{*}: cxx.poptions += -DOPENCV2_SHARED_BUILD

# Export options.
#
lib{opencv-stereo}:
{
  cxx.export.poptions = "-I$out_pfx_inc" "-I$src_pfx_inc" -DHAVE_OPENCV_STEREO
  cxx.export.libs = $intf_libs
}

#liba{opencv-stereo}: cxx.export.poptions += -DOPENCV2_STATIC
#libs{opencv-stereo}: cxx.export.poptions += -DOPENCV2_SHARED

# For pre-releases use the complete version to make sure they cannot
# be used in place of another pre-release or the final version. See
# the version module for details on the version.* variable values.
#
if $version.pre_release
  lib{opencv-stereo}: bin.lib.version = "-$version.project_id"
else
  lib{opencv-stereo}: bin.lib.version = "-$version.major.$version.minor"

# Don't install private headers.
#
{hxx ixx txx}{*}: install = false
